
## Custom Config
escape-copy is a custom shell script for converting file path that contain [] into a queryable path thought Z Shell  
- it is embeded inside a custom "copy" keymap inside keymap.toml

## Custom Plugin
folder rules: for applying custom sorting format for specific folder(s), else set to specific format

## Flavor 
Nord, copied from the yazi flavor repository
